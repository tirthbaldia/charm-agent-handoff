name: Placeholder and Secrets Gate

on:
  push:
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate placeholders and secrets
        run: |
          python3 scripts/validate_placeholders.py
